<?php

// Define the function
function greet($name)
{
    return "Hello, $name!";
}

// Call the function with a name
$name = "John";
$greeting = greet($name);

// Output the greeting
echo $greeting;
